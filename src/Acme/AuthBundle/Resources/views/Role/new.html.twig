{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Role creation</h1>

    <form action="{{ path('auth_role_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('auth_role') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
